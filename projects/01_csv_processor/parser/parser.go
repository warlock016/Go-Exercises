package parser

import (
	"github.com/warlock016/csv_processor/config"
	"github.com/warlock016/csv_processor/types"
)

func ParseRawData(raw *types.RawData, cfg *config.ParserConfig) (*types.ParsedData, error) {

	return nil, nil
}
